<%= form_for(current_user.loves.build(loved_id: @micropost.id), :remote => true) do |f| %>

	<%= f.hidden_field :lover_id, :value => current_user.id %>
  	<%= f.hidden_field :loved_id, :value => @micropost.id %>
	<%= image_submit_tag('heartbutton2.png') %>
	
<% end %>



